type Query {
    user: User!
    organizations:[Organization!]!
    organization(id:ID!):Organization!
    metrics:[Metric!]
    metric(metricId:ID!):Metric!
    parameterGroup(parameterGroupID:ID!):ParameterGroup!
}
